// This file is auto-generated by @hey-api/openapi-ts

import type { Options as ClientOptions, TDataShape, Client } from '@hey-api/client-fetch';
import type { CreateAccountData, CreateAccountResponse, CreateAccountError, ListUnlinkedAccountsData, ListUnlinkedAccountsResponse, ListUnlinkedAccountsError, DeleteAccountData, DeleteAccountResponse, DeleteAccountError, LinkAccountData, LinkAccountResponse, LinkAccountError, UnlinkAccountData, UnlinkAccountResponse, UnlinkAccountError, ListConnectorsData, ListConnectorsResponse, ListConnectorsError, GetIngestionConfigData, GetIngestionConfigResponse, GetIngestionConfigError, ListIngestionRunsData, ListIngestionRunsResponse, ListIngestionRunsError, GetActiveRunsData, GetActiveRunsResponse, GetActiveRunsError, RunIngestionData, RunIngestionResponse, RunIngestionError, GetRunStatusData, GetRunStatusResponse, GetRunStatusError, ListUserSummariesData, ListUserSummariesResponse, ListUserSummariesError, GetUserSummaryData, GetUserSummaryResponse, GetUserSummaryError, BrowseMessagesData, BrowseMessagesResponse, BrowseMessagesError, DeleteNoteData, DeleteNoteResponse, DeleteNoteError, ListOrgNodesData, ListOrgNodesResponse, ListOrgNodesError, CreateOrgNodeData, CreateOrgNodeResponse, CreateOrgNodeError, DeleteOrgNodeData, DeleteOrgNodeResponse, DeleteOrgNodeError, UpdateOrgNodeData, UpdateOrgNodeResponse, UpdateOrgNodeError, AssignOrgMemberData, AssignOrgMemberResponse, AssignOrgMemberError, RemoveOrgMemberData, RemoveOrgMemberResponse, RemoveOrgMemberError, ListUsersData, ListUsersResponse, ListUsersError, CreateUserData, CreateUserResponse, CreateUserError, ForgetUserData, ForgetUserResponse2, ForgetUserError, GetUserData, GetUserResponse, GetUserError, UpdateUserData, UpdateUserResponse, UpdateUserError, ListUserAccountsData, ListUserAccountsResponse, ListUserAccountsError, ListUserMessagesData, ListUserMessagesResponse, ListUserMessagesError, ListUserNotesData, ListUserNotesResponse, ListUserNotesError, CreateUserNoteData, CreateUserNoteResponse, CreateUserNoteError } from './types.gen';
import { client as _heyApiClient } from './client.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = ClientOptions<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Attach an external account to a person by hand
 * For accounts a connector has not observed. The server synthesises the external id, because nobody has authenticated against this account and the platform has therefore not issued one.
 *
 * The first account attached to somebody becomes their primary; later ones do not displace it.
 */
export const createAccount = <ThrowOnError extends boolean = false>(options: Options<CreateAccountData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateAccountResponse, CreateAccountError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/accounts',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * List accounts nobody has been attributed to
 * External identities ingestion has seen but that no person has been matched to yet. Each carries how many messages it has sent and when it last spoke, which is what makes attributing it a decision rather than a guess.
 */
export const listUnlinkedAccounts = <ThrowOnError extends boolean = false>(options?: Options<ListUnlinkedAccountsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListUnlinkedAccountsResponse, ListUnlinkedAccountsError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/accounts/unlinked',
        ...options
    });
};

/**
 * Delete an account and every message that arrived on it
 * Destructive, and distinct from unlinking: this keeps nothing. The response counts the messages destroyed alongside it.
 */
export const deleteAccount = <ThrowOnError extends boolean = false>(options: Options<DeleteAccountData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteAccountResponse, DeleteAccountError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/accounts/{account_id}',
        ...options
    });
};

/**
 * Attribute an account to a person
 * Every message that ever arrived on this account is reattributed to that person. The record of which account each message actually came from is untouched, so provenance survives the move.
 */
export const linkAccount = <ThrowOnError extends boolean = false>(options: Options<LinkAccountData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<LinkAccountResponse, LinkAccountError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/accounts/{account_id}/link',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Detach an account from its person
 * The messages stay and return to the unresolved pool. Use this when an account was attributed to the wrong person - the history is preserved and can be reattributed.
 */
export const unlinkAccount = <ThrowOnError extends boolean = false>(options: Options<UnlinkAccountData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<UnlinkAccountResponse, UnlinkAccountError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/accounts/{account_id}/unlink',
        ...options
    });
};

/**
 * List every platform and what it has contributed
 * One row per platform, including platforms with nothing on them - a platform silently missing from this list would look identical to one that is working.
 *
 * `status` is **inferred** from stored data: how many accounts exist and how recently anything arrived. Nothing polls a connector or checks a credential, because no real connector exists yet, so a platform can read `connected` here while its token expired an hour ago.
 */
export const listConnectors = <ThrowOnError extends boolean = false>(options?: Options<ListConnectorsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListConnectorsResponse, ListConnectorsError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/connectors',
        ...options
    });
};

/**
 * Inspect one platform's active ingestion configuration
 * Confirms which prompt, model, executor and connector a deployment picked up.
 */
export const getIngestionConfig = <ThrowOnError extends boolean = false>(options: Options<GetIngestionConfigData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetIngestionConfigResponse, GetIngestionConfigError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/ingestion/config/{platform}',
        ...options
    });
};

/**
 * List past ingestion runs
 * Newest first, each with the filtering verdicts it recorded. That trail is what makes tuning the filter policy a reviewable act rather than a guess - it is the evidence behind every retention decision.
 *
 * Dry runs appear here too. They deliberately store no messages, but the record of what they *would* have stored is the entire point of running one.
 *
 * Unfiltered by default, across every platform. Pass `platform` to see just one pipeline's history.
 */
export const listIngestionRuns = <ThrowOnError extends boolean = false>(options?: Options<ListIngestionRunsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListIngestionRunsResponse, ListIngestionRunsError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/ingestion/runs',
        ...options
    });
};

/**
 * Is ingestion running right now, anywhere
 * For a console-wide indicator, not a dashboard - just whether at least one run is currently in flight, on any platform. Reads Temporal's live state directly rather than the history table, which only gains a row once a run finishes.
 *
 * Always answers `count: 0` when ingestion isn't orchestrated by Temporal - an inline run has already finished by the time its `POST` responds, so there's never anything left in flight to report.
 */
export const getActiveRuns = <ThrowOnError extends boolean = false>(options?: Options<GetActiveRunsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<GetActiveRunsResponse, GetActiveRunsError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/ingestion/runs/active',
        ...options
    });
};

/**
 * Queue one platform's ingestion cycle
 * One pipeline per platform, invoked by the external scheduler independently per platform. Pulls messages from that platform's connector, filters them with the agentic policy, generates embeddings, and stores the survivors.
 *
 * **Accepted, not completed.** The run is handed to a durable workflow and this returns immediately with a `run_id`. Poll `GET /ingestion/runs/{platform}/{run_id}` for stage-by-stage progress and the final counters. Inference against a local model takes minutes, so waiting for it in the request would time out in a browser long before there was anything to report.
 *
 * Idempotent twice over: messages already stored are skipped by `(platform, external_message_id)`, and the workflow id derived from `run_id` means the same run cannot be started concurrently.
 *
 * 404 if no connector is configured for the platform yet.
 */
export const runIngestion = <ThrowOnError extends boolean = false>(options: Options<RunIngestionData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<RunIngestionResponse, RunIngestionError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/ingestion/runs/{platform}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Poll a queued run
 * Stage-by-stage progress while a run is in flight, and the full report once it finishes. `status` is one of queued, running, completed, failed or cancelled; `result` is populated only on completion.
 *
 * Progress comes from a workflow query, which reads live state without replaying history, so polling this is cheap.
 */
export const getRunStatus = <ThrowOnError extends boolean = false>(options: Options<GetRunStatusData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetRunStatusResponse, GetRunStatusError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/ingestion/runs/{platform}/{run_id}',
        ...options
    });
};

/**
 * List users with agent-generated communication summaries
 * Returns a page of users together with a concise, agent-generated summary of their communication history.
 *
 * This response contains personal data: names, email addresses, linked third-party accounts, verbatim message excerpts and a behavioural summary. It requires both `insights:read` and `messages:read`.
 *
 * Summaries are generated per request and are not cached, so latency and cost scale with page size.
 */
export const listUserSummaries = <ThrowOnError extends boolean = false>(options?: Options<ListUserSummariesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListUserSummariesResponse, ListUserSummariesError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/insights/users',
        ...options
    });
};

/**
 * Summarise one person's communication history
 * The single-person counterpart to the list endpoint. Both bounds are optional and inclusive; omitting them summarises everything retained.
 *
 * Nothing is cached: every call reaches the model. That is free and deterministic under the stub provider and would not be with a real one, so persisting summaries against an input fingerprint is deliberate later work rather than an omission.
 *
 * A window containing no messages is not an error - it returns a `summary_error` explaining there was nothing to describe.
 */
export const getUserSummary = <ThrowOnError extends boolean = false>(options: Options<GetUserSummaryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetUserSummaryResponse, GetUserSummaryError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/insights/users/{user_id}/summary',
        ...options
    });
};

/**
 * Browse stored messages
 * Every filter is optional; omitting all of them returns the most recent messages. The date bounds are inclusive, and the search is a case-insensitive substring match against the message body - wildcard characters in it are treated literally.
 *
 * Offset-paged: `limit` and `offset` bound the query in the database, so the response size never grows with the size of the table.
 */
export const browseMessages = <ThrowOnError extends boolean = false>(options?: Options<BrowseMessagesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<BrowseMessagesResponse, BrowseMessagesError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/messages',
        ...options
    });
};

/**
 * Delete a note
 */
export const deleteNote = <ThrowOnError extends boolean = false>(options: Options<DeleteNoteData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteNoteResponse, DeleteNoteError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/notes/{note_id}',
        ...options
    });
};

/**
 * Fetch the whole department hierarchy
 * Returns every department flat, each carrying its own `parent_id` and the ids of the people filed into it. The client rebuilds the tree in one pass.
 *
 * Flat rather than nested on purpose: the response shape stays the same however deep the hierarchy gets, and moving a department is a one-field change rather than a restructure.
 */
export const listOrgNodes = <ThrowOnError extends boolean = false>(options?: Options<ListOrgNodesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListOrgNodesResponse, ListOrgNodesError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/org/nodes',
        ...options
    });
};

/**
 * Create a department
 */
export const createOrgNode = <ThrowOnError extends boolean = false>(options: Options<CreateOrgNodeData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateOrgNodeResponse, CreateOrgNodeError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/org/nodes',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Delete a department, promoting its children
 * Sub-departments move up to the deleted department's parent rather than being deleted or detached. The response says how many moved, which is worth showing in a confirmation: deleting a leaf and deleting a division are different acts.
 */
export const deleteOrgNode = <ThrowOnError extends boolean = false>(options: Options<DeleteOrgNodeData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteOrgNodeResponse, DeleteOrgNodeError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/org/nodes/{node_id}',
        ...options
    });
};

/**
 * Rename a department or move it in the hierarchy
 * Send `reparent: true` to apply `parent_id`, including null to make the department a root. Without it, `parent_id` would be ambiguous with 'leave the parent alone'.
 *
 * Send `position` to place it among its siblings - alone, this reorders among its current ones; combined with `reparent`, among its new ones (omitted, it appends to the end).
 *
 * Returns 422 if the move would make a department report to itself or to one of its own sub-departments - which would cut that branch loose from the organization.
 */
export const updateOrgNode = <ThrowOnError extends boolean = false>(options: Options<UpdateOrgNodeData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateOrgNodeResponse, UpdateOrgNodeError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/org/nodes/{node_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * File a person into a department
 * A move, not an add: a person belongs to exactly one department, so this removes any previous membership. The console is expected to confirm with the user first when the person already has one.
 */
export const assignOrgMember = <ThrowOnError extends boolean = false>(options: Options<AssignOrgMemberData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AssignOrgMemberResponse, AssignOrgMemberError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/org/nodes/{node_id}/members',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Remove a person from a department
 * Leaves them unassigned. Returns 404 if they are filed into a different department - silently succeeding there would let a stale screen unfile somebody the user was not looking at.
 */
export const removeOrgMember = <ThrowOnError extends boolean = false>(options: Options<RemoveOrgMemberData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<RemoveOrgMemberResponse, RemoveOrgMemberError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/org/nodes/{node_id}/members/{user_id}',
        ...options
    });
};

/**
 * List everyone in the directory
 * Returns every person with the counts the directory shows beside them: which platforms they have accounts on, how many messages are attributed to them, when they last said anything, and which department they are filed into.
 *
 * Unpaged by default - the roster is bounded by headcount, and most callers (the org chart, sender-name lookups, the account-linking picker) need it complete rather than one page of it. Pass `limit` (and optionally `offset`) to page through it instead, for a directory screen that would rather not render the whole roster at once; the total row count and whether more remains are reported on the `X-Total-Count` / `X-Has-More` response headers, since the body stays the same flat array either way.
 */
export const listUsers = <ThrowOnError extends boolean = false>(options?: Options<ListUsersData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListUsersResponse, ListUsersError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/users',
        ...options
    });
};

/**
 * Create a person by hand
 * For people a connector has not discovered. The email address is normalised to lower case before it is checked for duplicates, because it is the key identity resolution merges on.
 *
 * Returns 409 if somebody already holds that address.
 */
export const createUser = <ThrowOnError extends boolean = false>(options: Options<CreateUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateUserResponse, CreateUserError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/users',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Erase a person and everything retained about them
 * Right to be forgotten. Removes the person, their linked accounts, their notes and every message attributed to them. Irreversible.
 *
 * The response counts what was destroyed, and is the only record that survives the deletion - worth keeping wherever the request came from.
 */
export const forgetUser = <ThrowOnError extends boolean = false>(options: Options<ForgetUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<ForgetUserResponse2, ForgetUserError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/users/{user_id}',
        ...options
    });
};

/**
 * Fetch one person's record
 */
export const getUser = <ThrowOnError extends boolean = false>(options: Options<GetUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetUserResponse, GetUserError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/users/{user_id}',
        ...options
    });
};

/**
 * Update a person's record
 * A partial update: only the fields present in the body are changed. Clear a text field by sending an empty string rather than null.
 */
export const updateUser = <ThrowOnError extends boolean = false>(options: Options<UpdateUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateUserResponse, UpdateUserError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/users/{user_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * List the external accounts attributed to one person
 */
export const listUserAccounts = <ThrowOnError extends boolean = false>(options: Options<ListUserAccountsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListUserAccountsResponse, ListUserAccountsError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/users/{user_id}/accounts',
        ...options
    });
};

/**
 * List the messages attributed to one person
 */
export const listUserMessages = <ThrowOnError extends boolean = false>(options: Options<ListUserMessagesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListUserMessagesResponse, ListUserMessagesError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/users/{user_id}/messages',
        ...options
    });
};

/**
 * List the notes written about one person
 */
export const listUserNotes = <ThrowOnError extends boolean = false>(options: Options<ListUserNotesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListUserNotesResponse, ListUserNotesError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/users/{user_id}/notes',
        ...options
    });
};

/**
 * Write a note about a person
 * Notes are what a human observed, kept separate from the generated summaries: those are derived and can be rebuilt, these cannot.
 */
export const createUserNote = <ThrowOnError extends boolean = false>(options: Options<CreateUserNoteData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateUserNoteResponse, CreateUserNoteError, ThrowOnError>({
        security: [
            {
                name: 'X-API-Key',
                type: 'apiKey'
            },
            {
                name: 'X-Dev-User',
                type: 'apiKey'
            }
        ],
        url: '/api/v1/users/{user_id}/notes',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};